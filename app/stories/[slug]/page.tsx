import { notFound } from "next/navigation"; import Link from "next/link"; import { ArrowLeft } from "lucide-react"; import { Header, Footer } from "../../components"; import { stories } from "../../content";
export function generateStaticParams(){return stories.map(s=>({slug:s.slug}));}
export default async function StoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const s=stories.find(x=>x.slug===slug); if(!s) notFound(); return <main><Header/><article><div className="container-wide py-16"><Link href="/stories" className="inline-flex items-center gap-2 text-sm font-bold text-blue"><ArrowLeft size={16}/> Back to Stories & Press</Link><p className="eyebrow mt-12 text-green">{s.type} · {s.date}</p><h1 className="display mt-5 max-w-5xl text-5xl leading-tight text-maroon md:text-7xl">{s.title}</h1><p className="mt-7 max-w-3xl text-xl leading-8 text-ink/65">{s.excerpt}</p></div><div className="container-wide aspect-[16/7] overflow-hidden bg-cream"><img src={s.image} alt="" className="h-full w-full object-cover"/></div><div className="container-wide grid gap-12 py-20 md:grid-cols-[1fr_300px]"><div className="max-w-3xl space-y-6 text-lg leading-8 text-ink/75">{s.body.map(p=><p key={p}>{p}</p>)}</div><aside className="h-fit bg-cream p-7"><p className="eyebrow text-green">Project category</p><p className="mt-3 font-extrabold text-maroon">{s.category}</p>{s.partner&&<><p className="eyebrow mt-7 text-green">Partner</p><p className="mt-3 font-extrabold text-maroon">{s.partner}</p></>}</aside></div></article><Footer/></main>}
